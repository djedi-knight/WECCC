import React from 'react';
import MyGroupsHeader from '../MyGroupsHeader';

export const Test = React.createClass({
  render() {
    return (
      <div>
        <MyGroupsHeader />
      </div>
    );
  }
});
