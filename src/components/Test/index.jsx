import React from 'react';
import ScoreBoxModal from '../ScoreBoxModal';

export const Test = React.createClass({
  render() {
    return (
      <div>
        <ScoreBoxModal />
      </div>
    );
  }
});
