import React from 'react';
import LoginModalView from '../LoginModalView';

export const Test = React.createClass({
  render() {
    return (
      <div>
        <LoginModalView />
      </div>
    );
  }
});
